<%= form_for(@cst) do |f| %>
  <% if @cst.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cst.errors.count, "error") %> prohibited this cst from being saved:</h2>

      <ul>
      <% @cst.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    Código: <br />
    <%= f.text_field :codigo %>
  </div>
  <div class="field">
    Nome: <br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit "Gravar" %>
  </div>
<% end %>
