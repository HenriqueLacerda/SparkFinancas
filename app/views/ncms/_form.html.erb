<%= form_for(@ncm) do |f| %>
  <% if @ncm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ncm.errors.count, "error") %> prohibited this ncm from being saved:</h2>

      <ul>
      <% @ncm.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codigo %><br />
    <%= f.text_field :codigo %>
  </div>
  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
